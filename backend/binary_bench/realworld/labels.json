[
  {
    "binary": "rw_openssl",
    "source_path": "/opt/homebrew/bin/openssl",
    "present_asymmetric": true,
    "families": ["RSA", "ECC", "ECDH", "DSA", "DH"],
    "linkage": "dynamic",
    "note": "Homebrew OpenSSL CLI, arm64 Mach-O, dynamically links libcrypto/libssl. Imports the full asymmetric API surface. Expect HIGH-confidence detection."
  },
  {
    "binary": "rw_ssh",
    "source_path": "/usr/bin/ssh (arm64 slice)",
    "present_asymmetric": true,
    "families": ["RSA", "ECDSA", "ECDH", "DSA", "DH"],
    "linkage": "dynamic",
    "note": "Apple OpenSSH client, dynamic libcrypto. Expect HIGH-confidence detection via imports."
  },
  {
    "binary": "rw_docker",
    "source_path": "/usr/local/bin/docker",
    "present_asymmetric": true,
    "families": ["ECDSA", "RSA", "ECDH"],
    "linkage": "static",
    "note": "Go static binary (go build, symbol table intact). Contains crypto/ecdsa, crypto/rsa, crypto/ed25519, crypto/elliptic. NOW DETECTED via the Go symbol path (qv_apis Go patterns + looks_like_go): NO OpenSSL symbols/constants, but the Go package-path symbols (crypto/ecdsa.*, crypto/rsa.*) are present and the linker's dead-code elimination means they are linked-and-callable. Expect HIGH-confidence detection. (A go build -ldflags=\"-s -w\" build would strip these and currently be missed — the gopclntab gap.)"
  },
  {
    "binary": "rw_ls",
    "source_path": "/bin/ls (arm64 slice)",
    "present_asymmetric": false,
    "families": [],
    "linkage": "dynamic",
    "note": "Plain coreutils-style tool, no asymmetric crypto. Expect clean negative."
  },
  {
    "binary": "rw_true",
    "source_path": "/usr/bin/true (arm64 slice)",
    "present_asymmetric": false,
    "families": [],
    "linkage": "dynamic",
    "note": "Trivial binary, no crypto. Expect clean negative."
  }
]
